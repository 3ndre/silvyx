// @mui
import { Box } from '@mui/material';

// ----------------------------------------------------------------------

export default function WalletIllustration({ ...other }) {
 

  return (
    <Box {...other}>
      <img src="/img/wallet.png" alt="wallet"/>
    </Box>
  );
}
